<ui:composition template="/WEB-INF/plantillas/general.xhtml"
                xmlns="http://www.w3.org/1999/xhtml"
                xmlns:ui="http://xmlns.jcp.org/jsf/facelets"
                xmlns:daw="jakarta.faces.composite/vistasComponentes"
                xmlns:h="http://java.sun.com/jsf/html"
                xmlns:f="http://java.sun.com/jsf/core">

    <ui:define name="contenido">

        <h1>Catálogo de Libros</h1>

        <h:form>

            <h:dataTable styleClass="table" var="l" value="#{libroCtrl.librosPaginados}">

                <h:column>
                    <div class="row libroFila">
                        <div class="col-md-2">
                            <img src="#{resource[l.imagen]}" class="imagenTablaLibros" alt="Imagen del Libro" />
                        </div>

                        <div class="col-md-8">
                            <div><b>ISBN:</b> #{l.isbn}</div>
                            <div><b>Título:</b> #{l.titulo}</div>
                            <div><b>Autor:</b> #{l.autor}</div>
                            <div><b>Género:</b> #{l.genero}</div>
                            <div><b>Sinopsis:</b> #{l.sinopsis}</div>
                        </div>

                        <div class="col-md-2">
                            <h:commandButton styleClass="button is-primary" value="Añadir al carrito" action="#{clienteCtrl.adicionC(l.isbn,request.userPrincipal.name)}" />
                        </div>

                    </div>
                </h:column>

            </h:dataTable>

            <!-- Controles de paginación -->
            <nav aria-label="Paginación de libros">
                <ul class="pagination justify-content-center">
                    <li class="page-item #{libroCtrl.paginaActual == 1 ? 'disabled' : ''}">
                        <h:commandLink styleClass="page-link" value="Primera" action="#{libroCtrl.primeraPagina}" />
                    </li>
                    <li class="page-item #{libroCtrl.paginaActual == 1 ? 'disabled' : ''}">
                        <h:commandLink styleClass="page-link" value="Anterior" action="#{libroCtrl.paginaAnterior}" />
                    </li>
                    <li class="page-item disabled">
                        <span class="page-link">Página #{libroCtrl.paginaActual} de #{libroCtrl.totalPaginas}</span>
                    </li>
                    <li class="page-item #{libroCtrl.paginaActual == libroCtrl.totalPaginas ? 'disabled' : ''}">
                        <h:commandLink styleClass="page-link" value="Siguiente" action="#{libroCtrl.paginaSiguiente}" />
                    </li>
                    <li class="page-item #{libroCtrl.paginaActual == libroCtrl.totalPaginas ? 'disabled' : ''}">
                        <h:commandLink styleClass="page-link" value="Última" action="#{libroCtrl.ultimaPagina}" />
                    </li>
                </ul>
            </nav>

        </h:form>

    </ui:define>

</ui:composition>
